#check own open ports
sudo nmap localhost

# Start your server
sudo systemctl start ssh

#Check service status
sudo systemctl status mysql(service)

#Stop service
sudo systemctl stop mysql(service)

#
ifconfig | grep 172.20.10.76(inet)
